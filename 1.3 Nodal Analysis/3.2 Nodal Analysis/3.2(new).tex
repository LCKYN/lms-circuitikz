\input{../../preamble.tex}
\begin{document}
\begin{circuitikz}[american]

    \draw (-2,4) to[I, l=\isname{1}, *-*] (2,4) 
                 to[R, l_=\rname{1}, -] ++(0,-4)
                 to[short, *-*] (0,0) node[ground]{};

    \draw (0,0) to[short] (-4,0) 
                to[R=\rname{1}] ++(0,4) 
                to[short] (-2,4)
                to[R,l_=\rname{2},-*] ++(0,-4);

    \draw (2,0) to[short] (4,0) 
                to[I, l=\isname{2}] (4,4) 
                to[short] ++(-2,0) 
                to[short] ++(0,2) 
                to[R,l_=\rname{3}] ++(-4,0) 
                to[short] ++(0,-2);

    % find
    % \draw[circuitikz/current arrow color=magenta](14,0) to[R, l=$R_4$, f_<=$\color{magenta}{i_4}$] ++(0,5);
    \nodev{-2}{4}{1};
    \nodev{2}{4}{2};
\end{circuitikz}
\end{document}