\input{../../preamble.tex}
\begin{document}
\begin{circuitikz}[american]
    %%% vertical %%%
    % \draw(0,0) [I] ++(0,4);
    
    \draw (0,0) to[I,l=\isname{1}] ++(0,4);
 
    \draw[circuitikz/current arrow color=magenta](2,0)  to[R, l=\rname{1}, f_<=$\color{magenta}{i_1}$] ++(0,4);
    \draw[circuitikz/current arrow color=magenta](4,0)  to[R, l=\rname{2}, f_<=$\color{magenta}{i_2}$] ++(0,4);
    \draw[circuitikz/current arrow color=magenta](8,0)  to[R, l=\rname{3}, f_<=$\color{magenta}{i_3}$] ++(0,4);
    \draw[circuitikz/current arrow color=magenta](10,0)  to[R, l=\rname{4}, f_<=$\color{magenta}{i_4}$] ++(0,4);

    \draw(12,0) to[I,l=\isname{3}] ++(0,4);

    % %%% horizontal %%%
    \draw(0,0)  to[short,-*] ++(2,0)
                to[short,-*] ++(2,0)
                to[short,-*] ++(4,0)
                to[short,-*] ++(2,0)
                to[short] ++(2,0);

    \draw(0,4)  to[short,-*] ++(2,0)
                to[short,-*] ++(2,0)
                to[I,l=\isname{2}] ++(4,0)
                to[short,-*] ++(2,0)
                to[short] ++(2,0);

    % %%% find %%%
    
\end{circuitikz}
\end{document}