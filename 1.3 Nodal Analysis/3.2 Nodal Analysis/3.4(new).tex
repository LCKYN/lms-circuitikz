\input{../../preamble.tex}
\begin{document}
\begin{circuitikz}[american]
    %%% vertical %%%
    % \draw(0,0) [I] ++(0,4);
    
    \draw (0,0) to[I,l=\isname{1}] ++(0,4);
 
    \draw[circuitikz/current arrow color=magenta](2,0)  to[R, l=\rname{1}, f_<=$\color{magenta}{i_1}$] ++(0,4);
    \draw[circuitikz/current arrow color=magenta](4,0)  to[R, l=\rname{2}, f_<=$\color{magenta}{i_2}$] ++(0,4);
    \draw[circuitikz/current arrow color=magenta](8,0)  to[R, l=\rname{3}, f_<=$\color{magenta}{i_3}$] ++(0,4);
    \draw[circuitikz/current arrow color=magenta](10,0)  to[R, l=\rname{4}, f_<=$\color{magenta}{i_4}$] ++(0,4);

    \draw(12,0) to[I,l=\isname{3}] ++(0,4);

    % %%% horizontal %%%
    \draw(0,0)  to[short,-*] ++(2,0)
                to[short,-*] ++(2,0)
                to[short,-*] ++(4,0)
                to[short,-*] ++(2,0)
                to[short] ++(2,0);

    \draw(0,4)  to[short,-*] ++(2,0)
                to[short,-*] ++(2,0)
                to[I,l=\isname{2}] ++(4,0)
                to[short,-*] ++(2,0)
                to[short] ++(2,0);

    % %%% find %%%
    % \nodev{2}{4}{1};
    % \nodev{6}{4}{2};


% \draw(0,0)  to[short] ++(0,5);
    
%     \draw (17,0) to[american current source,l=$I_{S3}$] ++(0,5);
%     \draw (0,0) to[short,-*] ++(3,0) to[short,-*] ++(3,0) to[short,-*] ++(5,0) to[short,-*] ++(3,0) to[short,-] ++(3,0);
%     \draw (17,5) to[short,-*] ++(-3,0) to[short,-*] ++(-3,0) to[american current source,l_=$I_{S2}$,-*] ++(-5,0) to[short,-*] ++(-3,0) to[short,-] ++(-3,0);
%     \draw(17,0) to[short] ++(0,5);

%     % find

%     \draw[circuitikz/current arrow color=magenta](3,0)  to[R, l=$R_1$, f_<=$\color{magenta}{i_1}$] ++(0,5);
%     \draw[circuitikz/current arrow color=magenta](6,0)  to[R, l=$R_2$, f_<=$\color{magenta}{i_2}$] ++(0,5);
%     \draw[circuitikz/current arrow color=magenta](11,0) to[R, l=$R_3$, f_<=$\color{magenta}{i_3}$] ++(0,5);
%     \draw[circuitikz/current arrow color=magenta](14,0) to[R, l=$R_4$, f_<=$\color{magenta}{i_4}$] ++(0,5);
%     % \draw(0,3) node[circ, color=magenta]{} node[anchor=south west,color=magenta] {$v_1$};



\end{circuitikz}
\end{document}