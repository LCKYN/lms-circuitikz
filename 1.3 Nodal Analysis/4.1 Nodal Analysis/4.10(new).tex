\input{../../preamble.tex}
\begin{document}
\begin{circuitikz}[american]

    %%% vertical %%%
    \draw (0,0) to[I, l=\isname{1}] ++(0,6) ;

    \draw (2,0) to[R, l=\rname{3}] ++(0,3) 
                to[R, l=\rname{2}] ++(0,3) ;

    \draw (6,0) to[R, l=\rname{4}] ++(0,6) ;

    \draw (8,0) to[I, l=\isname{2}] ++(0,6) ;

    %%% horizontal %%%
    \draw (0,0) to[short,-*] ++(2,0) 
                to[short,-*] ++(2,0) node[ground]{} 
                to[short,-*] ++(2,0) 
                to[short,-] ++(2,0);
    
    \draw (0,6) to[short] ++(2,0) 
                to[R,l=\rname{1}] ++(4,0) 
                to[short] ++(2,0);
 
    %%% find %%%
    \nodev{2}{6}{1};
    \nodev{6}{6}{2};

\end{circuitikz}
\end{document}