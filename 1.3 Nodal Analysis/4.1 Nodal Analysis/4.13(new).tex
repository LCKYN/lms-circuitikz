\input{../../preamble.tex}
\begin{document}
\begin{circuitikz}[american]
    %%% vertical %%%
    \draw (0,0) to[I, l=\isname{1}] ++(0,4) 
                to[short] ++(0,3) ;

    \draw (4,0) to[R, l=\rname{2}] ++(0,4) ;

    \draw (8,7) to[short] ++(0,-3) 
                to[I, l_=\isname{2}] ++(0,-4) ;

    \draw (10,0) to[R, l=\rname{5}] ++(0,4) ;

    %%% horizontal %%%
    \draw(0,0)  to[short,-*] ++(4,0) 
                to[short,-*] ++(4,0) 
                to[short] ++(2,0);

    \draw(0,4)  to[R, l=\rname{1}] ++(4,0) 
                to[R, l=\rname{4}] ++(4,0) 
                to[short] ++(2,0);

    \draw (0,7)  to[short] ++(4,0) 
                to[R, l=$R_3$] ++(4,0) ;

    %% %% %%
    \draw (3,4)   to[short, *-] ++(0,1.5) 
                    to[american current source, l_=\isname{3}] ++(-2,0) 
                    to[short, -*] ++(0,-1.5);
    %%% find %%%
    \nodev{0}{4}{1};
    \nodev{4}{4}{2};
    \nodev{8}{4}{3};

\end{circuitikz}
\end{document}