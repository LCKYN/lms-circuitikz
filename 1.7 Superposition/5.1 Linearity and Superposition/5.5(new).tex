\input{../../preamble.tex}
\begin{document}
\begin{circuitikz}[american,cute inductors]
    \input{../../preamble2.tex}

    \draw(0,0)
        to[short] ++(-2,0)
        to[I,l=\isname{1}] ++(0,6)
        to[short] ++(2,0)
        to[R,l_=\rname{1}] ++(0,-3)
        to[R,l_=\rname{2}] ++(0,-3)
        to[short,*-*] ++(4,0)
        to[R,l_=\rname{4}] ++(0,6)
        to[R,l_=\rname{3},*-*] ++(-4,0) (4,6)
        to[short] ++(2,0)
        to[I,l=\isname{2}] ++(0,-6)
        to[short] ++(-2,0);

    %%% find %%%
    % \node at(3.7,4.6) []{$t=0$};
    % \node at(9,6) []{$v_o$};
    % \draw[magenta](6.7,2.7)  
        % to[open,v=$v$] ++(2,0);

    % \draw[magenta](8.1,3)  
    %     to[open,v^=$v_x$] ++(0,-2);

    \draw[circuitikz/current arrow color=magenta](3.8,6)
    to[open,f>^=$\color{magenta}{i}$] ++(-2,0);
\end{circuitikz}
\end{document}